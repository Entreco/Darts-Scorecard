<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" alias="View" />
        <variable
            name="animator"
            type="nl.entreco.dartsscorecard.tv.launch.LaunchTvAnimator" />

        <variable
            name="navigator"
            type="nl.entreco.dartsscorecard.tv.launch.LaunchTvNavigator" />

        <variable
            name="viewModel"
            type="nl.entreco.dartsscorecard.tv.launch.LaunchTvViewModel" />
    </data>

    <android.support.v7.widget.FitWindowsFrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/splash">

        <org.webrtc.SurfaceViewRenderer
            android:id="@+id/remoteView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- Nested LinearLayout with overscan-safe margin -->
        <android.support.v7.widget.LinearLayoutCompat
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="27dp"
            android:layout_marginLeft="48dp"
            android:layout_marginRight="48dp"
            android:layout_marginTop="27dp"
            android:gravity="center_horizontal"
            android:orientation="vertical"
            android:animateLayoutChanges="true"
            android:paddingTop="@dimen/xlarge">

            <include
                android:id="@+id/include_launch_header"
                app:showIf="@{!viewModel.isStreaming}"
                layout="@layout/include_launch_header" />

            <View
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1" />

            <android.support.v4.widget.ContentLoadingProgressBar
                style="?android:attr/progressBarStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_above="@id/logo"
                android:layout_below="@id/include_launch_header"
                app:showIf="@{viewModel.isLoading}" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_above="@id/logo"
                android:layout_below="@id/include_launch_header"
                android:text="@string/enter_code_on_phone"
                app:showIf="@{!viewModel.isLoading &amp;&amp; !viewModel.isStreaming}"
                android:textColor="@color/gray"
                android:textSize="20sp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_above="@id/logo"
                android:layout_below="@id/include_launch_header"
                android:text="@{viewModel.registrationCode}"
                app:showIf="@{!viewModel.isLoading &amp;&amp; !viewModel.isStreaming}"
                android:textColor="@color/white"
                android:textSize="50sp"
                android:padding="@dimen/def"
                tools:text="2243"/>

            <View
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1" />

            <ImageView
                android:id="@+id/logo"
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:layout_alignParentBottom="true"
                android:src="@drawable/ic_entreco_black_bg" />

        </android.support.v7.widget.LinearLayoutCompat>

        <!-- Nested FrameLayout with overscan-safe margin -->
        <FrameLayout
            android:id="@+id/matchContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="27dp"
            android:layout_marginLeft="48dp"
            android:layout_marginRight="48dp"
            android:layout_marginTop="27dp"
            android:gravity="center_horizontal"
            app:attachMatch="@{viewModel.isStreaming}"
            app:launchNavigator="@{navigator}"
            android:visibility="@{viewModel.isStreaming ? View.VISIBLE : View.GONE }"
            android:orientation="vertical"
            android:animateLayoutChanges="true"
            android:paddingTop="@dimen/xlarge" />

    </android.support.v7.widget.FitWindowsFrameLayout>
</layout>