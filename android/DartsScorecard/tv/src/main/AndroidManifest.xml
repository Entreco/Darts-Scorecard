<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nl.entreco.dartsscorecard.tv">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application android:banner="@drawable/tv_promo">
        <activity
            android:name=".splash.SplashTvActivity"
            android:banner="@drawable/tv_promo"
            android:icon="@drawable/tv_promo"
            android:label="@string/app_name"
            android:logo="@drawable/tv_promo"
            android:noHistory="true"
            android:screenOrientation="landscape"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".launch.LaunchTvActivity" />
    </application>

</manifest>